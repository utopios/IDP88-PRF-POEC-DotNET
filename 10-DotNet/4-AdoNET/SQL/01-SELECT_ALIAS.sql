-- Requête pour voir l'ensemble des données d'une table (Product_Type)
SELECT * FROM Product_Type

-- Requête pour voir l'ensemble des données d'une table (Officer)
SELECT * FROM Officer

-- Possibilité de donner un alias pour une table
SELECT Pty.* FROM Product_Type Pty

-- Possibilité de donner un alias pour une table
SELECT Alias.* FROM Product_Type Alias